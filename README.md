Setup:

1. Open the project using terminal
2. use C# devkit extention and .NET MAUI.
3. use command: dotnet workload restore and dotnet maui install
4. for android deployment write aab on the android.csproj file instead of apk
5. use C# Facecheck.android and C# Facecheck.iOS on the vscode
